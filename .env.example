# Application Environment Variables
# The application uses a local, self-contained SQLite database (.data/database.sqlite).
# No external database connection (DATABASE_URL) is needed.
GEMINI_API_KEY=
